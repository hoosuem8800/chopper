import React, { useState, useEffect } from 'react';
import { useAuth } from '@/contexts/AuthContext';
import { API_BASE_URL, api, getProfilePictureUrl } from '@/services/api';

const UserDebug = () => {
  // Always return null to hide the debug component
  return null;
};

export default UserDebug; 